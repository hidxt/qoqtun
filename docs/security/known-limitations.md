# 已知安全限制与接受项（Phase 16）

以下项经审计评估为可接受（P2 或固有），记录在案；任何变更需重新评估。

| 项 | 类别 | 说明 | 缓解/后续 |
|---|---|---|---|
| CA/Server 密钥物理泄露 | T17 残余 | 泄露=信任域重建 | 离线/加密备份；CA 轮换预案；发布流程强化 |
| 恶意 Server 可读明文 | T18 固有 | 内网穿透架构固有（Server 中转） | 文档明示信任模型；HTTPS 等端到端加密建议 |
| L3/L4 超大流量洪泛 | T9 剩余 | 超出软件能力 | 上游/机房 DDoS 防护；文档说明 |
| 标准库漏洞（Go 1.26.5） | A1 | crypto/tls、x509、asn1 | **升级 Go ≥1.26.6**；CI 已用 1.26 自动补丁 |
| 10k 并发浸泡（本机） | 测试覆盖 | Windows 回环 fd/handle 资源上限 | CI/Linux 承担；基准保留 |
| 1000 并发基准（Windows） | 测试覆盖 | 同上 | 同上 |
| 端口释放延迟（Windows UDP） | A4 | 关闭后短暂占用 | UDP bind 重试（已实现） |
| 日志 trace 级可能含更多元数据 | T14 残余 | 仍不含密钥/载荷 | 文档警示 |
| Token 经 --token flag 会进 shell history | T17 残余 | 交互建议 stdin | 帮助文本+文档明示 |
| 本地控制端点（client tunnel 命令） | 新增面 | 127.0.0.1 + 0600 token 文件 | 本地信任模型；多用户服务器限制文件访问 |
