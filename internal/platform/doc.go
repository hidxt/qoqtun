// Package platform holds cross-platform capabilities (secure keystore,
// process hardening, etc.). Implementations land in later phases; the
// package exists so the monorepo layout (01-architecture.md §4) is complete
// from the start.
package platform
